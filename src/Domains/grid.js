import { Cell } from "./cell"

export class Grid {

    //GRID 3 * 3
    static generateGrid() {
        let length = 3 * 3
        return length
    }
    
    constructor(column, cell) {
        console.log(Cell)
        this.cell = Cell
    }
}
