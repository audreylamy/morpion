import React, { useContext } from "react"
import { WrapperCell } from "./style"

const Cell = ({key, cell, column}) => {

    return (
        <WrapperCell>
        
        </WrapperCell>
    )
}

export default Cell
